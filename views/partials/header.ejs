<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <nav>
        <img src="">
        <a href="/home" <%- title === 'Home Page' ? 'class="active"' : '' %>>Home</a>
        <% if (user) { %>
          <a href="/:id/profile"	<%- title === 'Profile' ? 'class="active"' : '' %>>
              Profile</a>
          <a href="/logout">LOG OUT</a>
        <% } else { %>
          <a href="/auth/google" class="login">LOG IN&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"></a>
        <% } %>
      </nav>